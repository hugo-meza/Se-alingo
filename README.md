# Se-alingo

Para subir videos u otros archivos grandes usar en git bash:
```bash
$ git lfs track "*.ext" 
```
donde ext es la extension de los archivos a subir. Después de esto, se sube en GitHub los archivos de forma normal
